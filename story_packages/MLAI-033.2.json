{
  "adr": "governance/adrs/ADR-0043-durable-marketing-workflow-spine.md",
  "blocked_decisions": [
    "workflow-ID golden vector and exact versioned domain-separated derivation format",
    "actor-reference derivation and truncation",
    "subcommand request-ID and timestamp derivation",
    "exact orchestration recovery and reconciliation algorithm",
    "approval-requester evidence lookup and digest validation",
    "transport-idempotency raw-key compatibility treatment, including exact accepted encoding, format and length validation"
  ],
  "classification": "Core",
  "implementation_authorized": false,
  "locked_decisions": [
    "Use one provider-neutral durable orchestration record for resumable planning-to-approval API operations.",
    "The orchestration record owns only the durable request claim, server-determined workflow identity, original canonical subcommand material, progress state and final safe HTTP response.",
    "Workflow state, approvals, workflow evidence, authorization audit, Campaign Plans and Marketing Briefs retain their existing authorities.",
    "API idempotency remains a completed-response cache; workflow receipts and evidence remain authoritative domain records.",
    "The idempotency key is client-generated and contains at least 128 bits of entropy; raw keys are never logged or returned; implementation defines and validates the accepted representation and length and adds golden tests, while exact encoding remains unresolved.",
    "Original canonical subcommand request IDs, timestamps, command kinds, expected versions and safe-command material are persisted before the first authority-changing command.",
    "Concurrent first submissions resolve through a durable uniqueness claim; exact retries reuse original canonical bytes and reconcile only missing steps; changed input conflicts deterministically.",
    "Approval recovery reuses an existing immutable approval and applies only a missing transition.",
    "The customer-facing boundary stops at approved and authorizes no execution or external effect.",
    "A future migration is required without changing historical migrations; retention and archival remain separately governed.",
    "HTTP contract version, MLAI-CJ-2 envelope schema version 2 and workflow safe-command schema version 1 remain distinct.",
    "Planning-to-approval POST operations retain existing session and CSRF requirements; technical details are omitted for callers without APPROVE."
  ],
  "migration_authorized": false,
  "out_of_scope": [
    "running or execution-oriented workflow transitions",
    "generation, publishing, spend, providers, external effects or learning",
    "application code, tests, database schema or migrations",
    "cloud, deployment, customer activation or release changes",
    "retention or archival policy selection"
  ],
  "rabbit": false,
  "schema_version": 2,
  "scope": [
    "durable provider-neutral planning-to-approval API orchestration authority",
    "pre-command request claim and canonical subcommand preservation",
    "concurrent first-submission resolution and deterministic retry reconciliation",
    "immutable-approval recovery followed only by a missing approved transition",
    "separate HTTP, MLAI-CJ-2 and workflow safe-command version domains",
    "tenant-authorized session-and-CSRF-protected operation ending at approved"
  ],
  "status": "governance-defined; implementation blocked and not authorized",
  "story_id": "MLAI-033.2",
  "title": "Tenant-Authorized Planning-to-Approval Workspace"
}
